{
  "mcpServers": {
    "threadlinqs-intelligence": {
      "command": "npx",
      "args": ["-y", "intelthreadlinqs-mcp@7.0.0"],
      "env": {
        "THREADLINQS_API_KEY": "${THREADLINQS_API_KEY}"
      }
    }
  }
}
